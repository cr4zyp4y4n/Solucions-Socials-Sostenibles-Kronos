/** Kit UI compartido (origen: módulo Firma). */
export {
  FirmaCard as KronosCard,
  FirmaButton as KronosButton,
  FirmaFieldLabel as KronosFieldLabel,
  FirmaInput as KronosInput,
  FirmaSelect as KronosSelect,
  FirmaTextarea as KronosTextarea,
  FirmaStatusBadge as KronosStatusBadge,
  FirmaTabs as KronosTabs,
  FirmaChip as KronosChip
} from '../firma/FirmaUi';

export { default as KronosModal } from '../firma/FirmaModal';
